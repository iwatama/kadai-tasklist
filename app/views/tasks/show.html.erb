<h1>id=
    <%= @task.id %>
    の詳細ページ
</h1>
    <table class="table table-bordered">
        <tr><th>id</th><td><%= @task.id %></td></tr>
        <tr><th>title</th><td><%= @task.title %></td></tr>
        <tr><th>content</th><td><%= @task.content %></td></tr>
        <tr><th>status</th><td><%= @task.status %></td></tr>
         
    </table>
    <%= link_to 'back to list', tasks_path ,class:'btn btn-default'%>
    <%= link_to 'update task', edit_task_path(@task), class:'btn btn-default' %>
    <%= link_to 'delete task',@task,method: :delete, data:{confirm:'本当に削除してよろしいですか'}, class:'btn btn-danger' %>

